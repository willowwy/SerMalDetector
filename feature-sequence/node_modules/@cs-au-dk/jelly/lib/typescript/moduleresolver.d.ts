import { FilePath } from "../misc/util";
export declare class TSModuleResolver {
    private readonly tsconfigCache;
    private getTSOptions;
    resolveModuleName(str: string, file: FilePath): FilePath;
}
