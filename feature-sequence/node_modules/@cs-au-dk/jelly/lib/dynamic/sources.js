"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isSourceSimplyWrapped = exports.isPathInTestPackage = void 0;
const TEST_PACKAGES = [
    "ava", "uvu", "yarn", "karma", "mocha", "jasmine", "tap", "tape", "@babel", "@jest",
    "nyc", "c8",
    "chai", "expect", "should", "supertest",
    "@sinonjs", "sinon", "nock",
];
const testRegexp = new RegExp(`\\bnode_modules/(?:${TEST_PACKAGES.join("|")}|(?:jest|istanbul)(?:-[^/]+)?)/`);
function isPathInTestPackage(path) {
    return testRegexp.test(path);
}
exports.isPathInTestPackage = isPathInTestPackage;
function isSourceSimplyWrapped(diskSource, observedSource) {
    const i = observedSource.indexOf(diskSource);
    if (i === -1)
        return false;
    const newlinePos = observedSource.indexOf("\n");
    return (newlinePos === -1 || i <= newlinePos);
}
exports.isSourceSimplyWrapped = isSourceSimplyWrapped;
//# sourceMappingURL=sources.js.map